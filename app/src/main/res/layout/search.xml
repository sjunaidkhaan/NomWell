<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_black_stroke_white_back"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imvwSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="@dimen/margin"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        android:padding="@dimen/padding"
        android:src="@drawable/ic_search_grey600_24dp" />

    <ImageView
        android:id="@+id/imvwRemove"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="@dimen/margin"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        android:padding="@dimen/padding"
        android:src="@drawable/ic_highlight_remove_grey600_24dp" />

    <android.support.v7.widget.AppCompatEditText
        android:id="@+id/txtSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@id/imvwRemove"
        android:layout_toRightOf="@id/imvwSearch"
        android:hint="@string/search"
        android:textSize="@dimen/text_size_normal" />
</RelativeLayout>