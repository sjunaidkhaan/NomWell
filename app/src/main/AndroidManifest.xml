<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zai.nomwell">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ChooseCityActivity"
            android:label="@string/title_activity_choose_city"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MySpotsActivity"
            android:label="@string/title_activity_my_spots"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListImportActivity"
            android:label="@string/title_activity_list_import"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddSpotsActivity"
            android:label="@string/title_activity_add_spots"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TabbedMapActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".StartingActivity"
            android:label="@string/title_activity_starting"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".IntroActivity"
            android:label="@string/title_activity_intro"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddNewSpotActivity"
            android:label="@string/title_activity_add_new_spot"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddTagActivity"
            android:label="@string/title_activity_add_tag"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TranslucentActivity" />
        <activity
            android:name=".SelectSpotActivity"
            android:label="@string/title_activity_select_spot"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SelectListActivity"
            android:label="@string/title_activity_select_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NomwellContactsActivity"
            android:label="@string/title_activity_nomwell_contacts"
            android:theme="@style/ContactsAppThemeNoActionBar" />
        <activity
            android:name=".ContactsSpotActivity"
            android:label="@string/title_activity_contacts_spot"
            android:theme="@style/ContactsAppThemeNoActionBar"></activity>
        <activity
            android:name=".ContactListSpotActivity"
            android:label="@string/title_activity_contact_list_spot"
            android:theme="@style/ContactsAppThemeNoActionBar"></activity>
    </application>

</manifest>
